version: "3.8"

services:
  jupyter:
    build: .
    container_name: reclamacoes-clientes-aeroportos
    volumes:
      - .:/app
    ports:
      - "8888:8888"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
