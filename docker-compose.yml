version: "3.8"

services:
  jupyter:
    build: .
    container_name: previsao_preco_imoveis
    volumes:
      - .:/app
    ports:
      - "8888:8888"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
